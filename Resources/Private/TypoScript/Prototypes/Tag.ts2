prototype(GesagtGetan.CookieDialogue:Tag) < prototype(TYPO3.TypoScript:Tag) {
  tagName = 'script'
  file = ${'resource://GesagtGetan.CookieDialogue/Public/Main.' + (this.tagName == 'script' ? 'js' : 'css')}
  @context.file = ${this.file}
  @if.file = ${!!this.file}

  content = GesagtGetan.CookieDialogue:FileContent {
    path = ${file}
  }
}
